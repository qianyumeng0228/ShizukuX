# Let's inspect the jar or how we can get waitForService.
# Since ServiceManager.waitForService isn't in the stub directly accessible to `javac`, we can use hidden API wrapper.
# Or use reflection if not accessible.
