plugins {
    id "com.android.library"
}

android {
    namespace = "af.shizuku.common"
    buildFeatures {
        buildConfig = false
    }
}

dependencies {
    implementation libs.timber
    implementation libs.androidx.annotation
    compileOnly libs.hidden.stub
}
